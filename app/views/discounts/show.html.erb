<%= image_tag @discount.image.url(:large) if @discount.image.present? %>
<h2><%= @discount.name %></h2>
<p><strong>Details:</strong> <%= @discount.description %></p>
<p><strong>Discount:</strong> <%= @discount.discount %></p>
<p><strong>Start Date:</strong> <%= @discount.start_date %></p>
<p><strong>End Date:</strong> <%= @discount.end_date %></p>
<p><strong>Location:</strong> <%= @discount.location %></p>

<p>By <%= link_to @discount.user&.full_name %></p>

<% if user_signed_in? && can?(:update, @discount) %>
  <%= link_to 'Edit', edit_discount_path(@discount) %>
  <%= link_to(
    'Delete',
    discount_path(@discount),
    data: {confirm: 'Are you sure?'},
    method: :delete
  ) %>
<% end %>
