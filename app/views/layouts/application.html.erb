<!DOCTYPE html>
<html>
  <head>
    <title>VancouverNow!</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <%= link_to('Vancouver Now!', root_path) %>
    <%# the aboveð generates: <a href='/'>Home</a> %>
    |
    <%= link_to 'Events', events_path %>
    |
    <%= link_to 'New Event', new_event_path %>
    |
    <%= link_to 'Discount', new_discount_path %>
    |
    <%= link_to 'Discounts', discounts_path %>
    <% if user_signed_in? %>
      Hello, <%= current_user.full_name %>
      <% if current_user.is_admin? %>
        <%= link_to 'Das
        hboard', admin_dashboard_index_path %>
      <% end %>

      <%= link_to 'Sign Out', sessions_path, method: :delete %>
    <% else %>
      <%= link_to "Sign In", new_session_path %>
      |
      <%= link_to "Sign Up", new_user_path %>
    <% end %>



    <% if flash[:notice].present? || flash[:alert].present? %>
    <div class="flash-message">
      <p><%= flash[:notice] || flash[:alert] %></p>
    </div>
    <% end %>
    <%= yield %>
  </body>
</html>
